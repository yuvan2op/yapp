{
  "name": "yapp-mern-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "api"
  ],
  "scripts": {
    "start": "npm run dev --workspace client",
    "dev": "npm run dev --workspace client",
    "api": "npm start --workspace api"
  }
}


